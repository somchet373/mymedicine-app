import { NextResponse } from "next/server";
export async function GET() { return NextResponse.json({ openapi: "3.0.3", info: { title: "MyMedicine REST API", version: "1.0.0", description: "JWT-protected API for medication management." }, paths: { "/api/auth/register": { post: { summary: "Register a user" } }, "/api/auth/login": { post: { summary: "Authenticate and set secure cookies" } }, "/api/auth/logout": { post: { summary: "Clear session" } }, "/api/medicines": { get: { summary: "List medicines with q, page and limit" }, post: { summary: "Create a medicine" } } }, components: { securitySchemes: { cookieAuth: { type: "apiKey", in: "cookie", name: "mm_access" } } } }); }
